/**
 * Content module: Hero spreads & single-Pokémon binders.
 *
 * Filled in by the "Hero spreads & single-Pokémon" content unit. Lean hardest into spans —
 * full-page (3×3 / 2×2) `artwork` spreads with a few card accents, and single-Pokémon books
 * across 6–8 pages. Helpers in ./_helpers. Add new cards to `cards`; binders to `binders`.
 */

import { type ContentModule } from '@/data/content/_helpers';

export const cards: ContentModule['cards'] = [];

export const binders: ContentModule['binders'] = [];
