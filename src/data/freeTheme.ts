/**
 * THE FREE THEME. One artwork theme is on the house for everyone, guest included: the Theme
 * Search button and the cheatsheet's recipe run it, and the `theme-search` function answers it
 * unmetered for any signed-in session (supabase/functions/theme-search, FREE_THEME). Kept in a
 * module with no imports so the kit configuration can read it without pulling the UI along.
 *
 * Forest because it is one of the three largest captioned scenes and the most page-like of them
 * (checked 2026-09-03).
 */
export const FREE_THEME = 'forest';
export const FREE_THEME_QUERY = `theme:${FREE_THEME}`;
