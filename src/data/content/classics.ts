/**
 * Content module: Vintage / WOTC classics binders.
 *
 * Filled in by the "Vintage / WOTC classics" content unit. Build michi-styled, multi-page
 * binders with the helpers in ./_helpers (card / artwork / insert / defineCard / page).
 * Add any new cards to `cards`; add binders to `binders`.
 */

import { type ContentModule } from '@/data/content/_helpers';

export const cards: ContentModule['cards'] = [];

export const binders: ContentModule['binders'] = [];
