/**
 * Content module: Color-theme study binders.
 *
 * Filled in by the "Color-theme studies" content unit. Build michi-styled, multi-page binders
 * with the helpers in ./_helpers (card / artwork / insert / defineCard / page) — lean on tonal
 * `insert` slots and intentional empty pockets. Add new cards to `cards`; binders to `binders`.
 */

import { type ContentModule } from '@/data/content/_helpers';

export const cards: ContentModule['cards'] = [];

export const binders: ContentModule['binders'] = [];
